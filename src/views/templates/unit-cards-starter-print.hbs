{{#extend 'layout/html' title='Starter Unit Cards Print' body_class="body-print"}}

    {{#content 'page-content'}}
        {{#> cards-print-header
            title='Unit Cards'
            print_all_url='/unit-cards-starter-print.html'
        }}

            {{#*inline 'view_url'}}
                {{~#if faction~}}
                    /unit-cards-starter.html#{{faction_slug}}
                    {{~else~}}
                    /unit-cards-starter.html
                {{~/if~}}
            {{/inline}}

            {{#*inline 'print_url'}}
                {{~#if faction~}}
                    /cards-print/{{faction_slug}}.html
                    {{~else~}}
                    /unit-cards-starter-print.html
                {{~/if~}}
            {{/inline}}

            {{#*inline 'pdf_url'}}
                {{~#if faction~}}
                    /assets/pdfs/{{faction_slug}}-cards-starter-print.pdf
                    {{~else~}}
                    /assets/pdfs/unit-cards-starter-print.pdf
                {{~/if~}}
            {{/inline}}

            {{#*inline 'other_faction_print_url'}}/unit-cards-starter-print/{{slug}}.html{{/inline}}

        {{/cards-print-header}}

        <div class="page-previews-container" data-bs-theme="light">
            <div class="output-container card-grid-container-to-scale-print">
                {{#each cardPages as |cardPage|}}

                    <div class="page page-preview page-letter" style="background-color:white">
                        <div class="page-card-grid">
                            {{#each cardPage as |cardData|}}
                                {{> (lookup cardData 'template') cardData}}
                            {{/each}}
                        </div>
                    </div>
                {{/each}}
            </div>
        </div>
    {{/content}}

{{/extend}}
