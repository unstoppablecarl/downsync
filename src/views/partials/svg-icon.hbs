<svg class="bi{{#if class}} {{class}}{{/if}}" {{#if size}}width="{{size}}" height="{{size}}"{{else}}width="32"
     height="32"{{/if}} fill="currentColor" aria-hidden="true" focusable="false">
    <use href="{{svg_icon_path icon}}"></use>
</svg>

